<nav class="bg-white border-b sticky top-0 shadow-md">
    <div
        class="max-w-screen-xl mx-auto px-4 py-2 flex justify-between items-center"
    >
        <a href="/" class="text-xl font-bold text-gray-800">
            <i class="fa-regular fa-compass"></i>
        </a>

        <button
            class="lg:hidden text-gray-800 focus:outline-none"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
        >
            <i class="fas fa-bars"></i>
        </button>

        <div class="hidden lg:flex space-x-6">
            <a href="/" class="text-gray-800 hover:text-blue-500">Home</a>
            <a href="/events/all" class="text-gray-800 hover:text-blue-500"
                >Events</a
            >

            <% if(currUser) { %>
            <a href="/students/logout" class="text-gray-800 hover:text-blue-500"
                >Log out</a
            >
            <% } %> <% if(!currUser) { %>
            <a
                href="/students/register"
                class="text-gray-800 hover:text-blue-500"
                >Register</a
            >
            <a href="/students/login" class="text-gray-800 hover:text-blue-500"
                >Log in</a
            >
            <% } %>
        </div>

        <div class="lg:hidden collapse" id="navbarNav">
            <div class="flex flex-col space-y-4 mt-4">
                <a href="/" class="text-gray-800 hover:text-blue-500">Home</a>
                <a href="/events" class="text-gray-800 hover:text-blue-500"
                    >Events</a
                >

                <% if(currUser) { %>
                <a href="/logout" class="text-gray-800 hover:text-blue-500"
                    >Log out</a
                >
                <% } %> <% if(!currUser) { %>
                <a href="/signup" class="text-gray-800 hover:text-blue-500"
                    >Sign up</a
                >
                <a href="/login" class="text-gray-800 hover:text-blue-500"
                    >Log in</a
                >
                <% } %>
            </div>
        </div>
    </div>
</nav>
